pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        //mpchart

        maven { url 'https://jitpack.io' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
      //  mpchart
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "MOODset"
include ':app'
